# FULL Servis — sunucu + agent ortak bağımlılıkları
fastapi
uvicorn[standard]
pydantic
requests
python-multipart

# Wi-Fi track (CPU/RAM/pil) ve torrent (qBittorrent süreç kontrolü) için
psutil

# YouTube'u EN YÜKSEK kaliteye zorlama (oynatıcı kalite menüsünü Selenium tıklatır).
# Selenium 4.6+ chromedriver'ı otomatik indirir; makinede Chrome/Chromium kurulu olmalı.
# Kurulu degilse YouTube basit tarayici acmaya geri duser (sistem yine calisir).
selenium

# Marka/Model/Firmware combobox'ları için firmware DB (cpeqadb, PostgreSQL/SSL).
# Bağlantı kurulamazsa sistem yine çalışır; alanlar serbest-metin girişine düşer.
sqlalchemy
psycopg2-binary

# Excel raporları (ping istatistik+grafik, wifi özet+grafik). Yalnızca SUNUCUDA
# (server/excel_service.py) kullanılır; import'lar lazy olduğu için bu paketler
# olmayan bir agent yine çalışır (sadece Excel üretilmez).
openpyxl
xlsxwriter
pandas
numpy
matplotlib

# macOS'ta Wi-Fi track icin ZORUNLU: CoreWLAN (Apple resmi Wi-Fi API'si) — kartin
# anlik durumunu TARAMASIZ okur, Windows'taki "netsh wlan show interfaces" gibi.
# YEDEK YOL YOK: kurulu degilse Wi-Fi izleme testi baslamaz ve net hata verir.
# (Eski yedek system_profiler'di; WI-FI TARAMASI yaptigi icin ayni karttan akan
#  iperf trafigini 630 -> 201 Mbit/sn'ye dusuruyordu, bu yuzden kaldirildi.)
'# BU SATIR BILEREK YORUMDA. Yalnizca wifi_track ROLU OLAN Mac'te (mac_wifi) gerekli;
# mac_cable'da gereksiz ve bazi makinelerde kurulumu patliyor (hazir wheel yoksa
# kaynaktan derlemeye kalkiyor). derle-mac.sh bunu AYRI olarak, hata tolere ederek
# kurar. Elle kurmak icin:  pip install pyobjc-framework-CoreWLAN
# pyobjc-framework-CoreWLAN; sys_platform == "darwin"
