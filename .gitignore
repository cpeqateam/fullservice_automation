# Python
__pycache__/
*.pyc
venv/
.venv/

# Node / Vite
node_modules/
dist/
.vite/

# Çalışma zamanı çıktıları
logs/
*.log

# PyInstaller paketleme çıktıları (derlenen uygulamalar USB ile dağıtılır, repoya girmez)
launchers/build/build/
launchers/build/dist/
launchers/build/cikti/

# Sırlar (firmware DB / FTP sertifikaları — repoya asla konmaz)
certs/
secrets.json
*.secrets.json

# OS / editör
.DS_Store

# GRK referans projesi — yalnızca yerel örnek, repoya gitmez
grk-automation/

# grk-test-platform — AYRI repo + sırlar (certs, .env) içerir; repoya ASLA gitmez
grk-test-platform/

image.png

# ── Depoya girmemesi gereken calisma dosyalari ──────────────────────────────
# Sunumlar (~20 MB) — kod deposunun isi degil, paylasimi ayri yapilir
*.pptx

# Ornek/gecici Excel ciktilari (iperf ozet ornegi vb.)
*.xlsx

# Derlenmis ikili dosyalar — kaynagi repoda yok, makineye ozel (Mach-O intel/arm)
client_intel
client_m1

# Tek basina duran yardimci scriptler — sistemin parcasi degil, hicbir yerden
# cagrilmiyor. (ornek_mac_script.py: system_profiler sorununu anlatan demo;
# iperf_visualizer_macos.py: elle calistirilailan gorsellestirici.)
ornek_mac_script.py
iperf_visualizer_macos.py
